//When the page first loads, the first text field should be in focus by default.

const name = document.getElementById('name');
name.focus();


//when you click on the other option from the job role the other textbox should appear
